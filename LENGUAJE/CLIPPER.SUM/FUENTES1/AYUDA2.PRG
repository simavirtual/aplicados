**** PROGRAMA: AYUDA2.PRG
SET KEY 28 TO ajuda
DO WHILE .T.
   cod = SPACE(3); nome = SPACE(30); valor = 0; nasc = CTOD("")
   ocupacao = SPACE(3); coment1 := coment2 := SPACE(60)
   CLEAR
   controle = 1
   @10,10 SAY "CODIGO..." GET cod
   @12,10 SAY "NOMBRE..." GET nome
   @14,10 SAY "SALARIO.." GET valor
   READ
   IF LASTKEY() = 27
      EXIT
   ENDIF
   CLEAR
   controle = 2
   @10,10 SAY "OCUPACION..." GET ocupacao
   @12,10 SAY "COMENTARIO.." GET coment1
   @14,10 SAY "            " GET coment2
   READ
ENDDO

PROCEDURE ajuda(prog,linha,var)
SAVE SCREEN TO telahelp
CLEAR

TEXT


	  ESTE SISTEMA  ESTA TOTALMENTE DOCUMENTADO EN EL MANUAL
	  DE OPERACION. LE ACONSEJAMOS UNA LECTURA COMPLETA
	  DEL MANUAL ANTES DE OPERAR EL SISTEMA.

	  DUDAS ESPECIFICAS PUEDEN RESOLVERSE POR MEDIO DEL
          CENTRO DE INFORMACION EXTENSION 223.

	  **** RECUERDE SIEMPRE  HACER COPIAS DE SEGURIDAD ****
	  ****   AL TERMINAR EL TRABAJO CON ESTE SISTEMA   ****


	       PRESIONE <ESPACIO> PARA VOLVER AL PROGRAMA
ENDTEXT

@ 0, 0 TO MAXROW(),MAXCOL()
INKEY(0)
RESTORE SCREEN FROM telahelp